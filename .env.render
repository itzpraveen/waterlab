# Environment Variables for Render.com Deployment
# Copy these to your Render Web Service Environment Variables

# Django Configuration
DJANGO_SETTINGS_MODULE=waterlab.settings_render
SECRET_KEY=your-generated-secret-key-here

# Database (Automatically provided by Render when you link the database)
# DATABASE_URL=postgresql://user:password@host:port/database

# Email Configuration (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password

# Performance Settings
WEB_CONCURRENCY=4

# Instructions:
# 1. Generate SECRET_KEY using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 2. Create Gmail App Password: https://support.google.com/accounts/answer/185833
# 3. Add these variables in Render Web Service > Environment tab
# 4. DATABASE_URL is automatically added when you link your PostgreSQL database